# app/config/security.yml
security:
    encoders:
        FOS\UserBundle\Model\UserInterface: bcrypt

    role_hierarchy:
        ROLE_ADMIN:       ROLE_USER
        ROLE_SUPER_ADMIN: ROLE_ADMIN

    providers:
        fos_userbundle:
            id: fos_user.user_provider.username

    firewalls:
        main:
            pattern: ^/
            form_login:
                provider: fos_userbundle
                csrf_provider: security.csrf.token_manager # Use form.csrf_provider instead for Symfony <2.4
            logout:       true
            anonymous:    true

    access_control:
        - { path: ^/login$, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/register, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/resetting, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/media/, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/api/, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/share/, role: IS_AUTHENTICATED_ANONYMOUSLY }
        # The public website. Narrow patterns on purpose: ^/pack/ and ^/reel/ are
        # the panel's own edit screens, which is why the site uses /stickers and
        # /reels instead.
        - { path: ^/$, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/home\.html, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/stickers, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/reels, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/sitemap\.xml, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/robots\.txt, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/privacy_policy, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/, role: ROLE_ADMIN }